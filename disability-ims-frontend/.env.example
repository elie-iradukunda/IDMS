# Base URL of the Disability Support IMS API.
# Leave empty in development — Vite proxies /api to http://localhost:4000
# (see vite.config.js). In production set it to the deployed API origin,
# e.g. VITE_API_URL="https://api.disability-ims.rw/api"
VITE_API_URL=
