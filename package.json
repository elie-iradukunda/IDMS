{
  "name": "idms",
  "version": "1.0.0",
  "private": true,
  "description": "Inclusive Disability Support IMS full-stack app",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm ci --prefix disability-ims-frontend && npm run build --prefix disability-ims-frontend && npm ci --omit=dev --prefix disability-ims-backend",
    "start": "npm start --prefix disability-ims-backend",
    "seed": "npm run seed --prefix disability-ims-backend"
  }
}
